// Get all elements that contain the body_tag attribute
const elements = document.querySelectorAll('[body_tag]');

// Filter elements where display is not 'none'
const visibleElements = Array.from(elements).filter(el => {
  const style = window.getComputedStyle(el);
  return style.display !== 'none';
});

console.log(visibleElements);
